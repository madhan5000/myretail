const utils = {
    '_message' : (code, message) =>{
        return {
            'error' : {
                'code'    : code,
                'message' : message
            }
        }
    },
    
}

export default utils;